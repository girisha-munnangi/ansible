- name: checking number is greter than 5 or not
  hosts: local
  connection: local
  vars:
    NUMBER: 3
  tasks:
  - name: is ansible more than 5 sessions
    ansible.builtin.debug:
      msg: "yes, more than 5 sessions"
    when: NUMBER > 5