- name: creating user with user module
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: user creating
    ansible.builtin.user:
      name: girisha

  - name: printing user 
    ansible.builtin.debug:
      msg: " {{ '(id girisha)'' }} "