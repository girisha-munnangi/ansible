- name : checking conditions
  hosts: local
  connection: local
  vars:
    IS_LIVE: false
  tasks:
  - name: checking true or false
    ansible.builtin.debug:
      msg: "yes it is true"
    when: IS_LIVE