- name : install packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name : installing packes 
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - mysql
    - nginx
    - zip