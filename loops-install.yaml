- name : install packages
  hosts: local
  connection: local
  tasks:
  - name : installing packes 
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - mysql
    - nginx
    - zip